StoryTime — Change History
==========================
Versioning guide:
  v0.x   = Pre-release / building toward MVP
  v1.0   = First fully working end-to-end app (generate, read, save, cross-device)
  v1.x   = Feature additions and polish after MVP
  v2.0   = Major redesign or architectural overhaul (rare)
  Minor rev (+0.1): new feature, meaningful UI change, new integration
  Patch rev (+0.0.1): bug fix, small tweak (optional — skip if change is trivial)

--------------------------
v0.6.6 — 2026-06-02 — Polish + Auto Thumbnails + Photo Cropping
  • New ingredients: 🏎️ Race, 🌟 Save the Day (max 3 selectable still)
  • Story ingredients now MRU-sorted (most recently used pills move up)
  • Photo capture redesigned: two separate buttons (📸 Take Photo / 🖼️ Upload Photo); photo state shows preview with ✕ overlay to delete (no more Replace/Remove confusion)
  • Photo cropping via Cropper.js — drag rectangle around main subject before AI analysis
  • Vision API prompt tightened: now describes ONLY the main subject, ignores background/surroundings
  • Photo description stored in separate collapsible field (not shoved into main description)
  • Both descriptions get combined automatically when Bring to Life runs
  • Auto-generated cartoon thumbnail on character save (~1¢ via gpt-image-1 low quality)
  • Thumbnail prompt produces centered head-and-shoulders portrait with breathing room for circle crop
  • "Regenerate Thumbnail" button in character edit for any character (existing ones included)
  • Default placeholder image (character-default.png) shown when no thumbnail
  • Character list shows circular thumbnails (replaces generic placeholder)
  • Genre + Artwork Style dropdowns now MRU-sorted (Surprise me always pinned at top)
  • Retro Video Game style: renamed from "Pixel / Video Game"; prompt emphasizes visible pixels + game-environment aesthetics
  • Chalkboard art style added (replaces Colored Pencil + Storybook Ink)
  • Paper Cutout style massively enhanced (kid-art-project feel with ripped edges, cardboard, wrinkles)
  • Pencil Sketch enhanced (more sketchy, looser lines, eraser marks; STRICTLY monochrome)
  • Removed: Slice of Life genre; Colored Pencil + Storybook Ink art styles
  • Settings: Image Generation now a 3-way radio (Generate all / Only first 2 / Skip all) — either-or
  • Settings: cleaned up labels (no "for parents", no "placeholders only")
  • New "Created By" form field with type-ahead suggestions (e.g. "Kai", "Kai & Papa"); X to delete suggestions; localStorage-backed
  • Cover now shows "By [name]" line at bottom when created_by is set
  • Cover layout in portrait fixed AGAIN with proper flex column + order property
  • Dynamic font size per story: text size scales based on average words per page (under 40 = 1.3rem, 100+ = 0.95rem); consistent across all pages of same story
  • Navigation arrows orientation-aware:
      Portrait → arrows at TOP and BOTTOM of book page (▲/▼); swipe UP for next, DOWN for previous
      Landscape → arrows LEFT and RIGHT centered (‹/›); swipe LEFT for next, RIGHT for previous
  • Bottom progress toast removed (will revisit in polish rev)
  • Cost estimate updates to reflect Image Generation mode (Skip = 0¢ images; First 2 = 2 × image cost)
  • Reference photo in character edit shown as SQUARE preview (not circular) — only thumbnails are circular
  • Photo + thumbnail blobs cleaned up on character delete
  • Build date corrected to 2026-06-02

--------------------------
v0.6.5 — 2026-06-01 — Photo Capture, Quiz, Search, Cost Transparency, Style Expansion
  • Photo capture for character creation/edit — Add Photo button uses camera (mobile) or file picker (desktop); GPT-4o Vision analyzes and merges into description; ~2¢ per photo
  • Photo preview shown in character form with Replace/Remove
  • Photo thumbnail shown in character list (replaces placeholder)
  • Photo blob stored in IndexedDB; deleted when character deleted or photo removed
  • Quiz/reflection generation — output in same story call (no extra cost): 3 comprehension multiple-choice + 2 reflection questions
  • Quiz Me button on Story Toolbox opens quiz modal with reveal-answer flow
  • One-sentence story summary generated with story; displayed under title on Story Toolbox
  • Character search input at top of list (shows when >3 characters); filters by name, tagline, description
  • Estimated cost display under Generate button (live updates as form changes)
  • Story Details box larger (6 rows, min-height bumped)
  • Side arrows moved to BOTTOM CORNERS of book page (no longer overlap text/image)
  • Loading progress as bottom popup toast with SVG progress ring (no text % — visual only); dismissible with X
  • Cover title position in portrait FIXED via CSS grid (real fix this time)
  • Modal scroll lock — opening any modal locks body scroll; closing restores
  • touch-action: pinch-zoom on images — allows LONG-PRESS to save image, blocks double-tap zoom
  • Removed -webkit-touch-callout: none from images (was preventing save dialog)
  • Smart badge logic: ❌ Failed only set if always_use_fallback AND failed AND never succeeded; ✓ counts both success and fail attempts; explains success rate when tapped
  • Genres added: Dinosaurs, Slice of Life, Underwater, Western (14 total)
  • Art styles added: Colored Pencil, Lego (building blocks), Stuffies (plush), Paper Cutouts, Storybook Ink (14 total)
  • Art styles enhanced: Pencil Sketch explicitly B&W; Crayon chunkier; Comic Book classic American; Anime more exaggerated eyes; Pixel renamed "Pixel / Video Game" 16-bit; Claymation more textured/lumpy
  • Lego style: "Lego" in UI but uses "interlocking plastic toy bricks" in prompt to avoid trademark
  • Build date updated to actual today (June 1)

--------------------------
v0.6.4 — 2026-05-30 — Polish Rev
  • Age picker: 2x2 grid (Toddler / Preschool / Early Reader / Older Kid) — bigger touch targets, labels fit on one line
  • Story Length: 3 min / 5 min / 8 min (4p / 6p / 9p) — more pages, accurate reading times
  • Story Ingredients: 2-column grid (perfect alignment, no ragged edges); Wonder → Battle
  • Genre converted to dropdown (matches Artwork Style); both use dice emoji for "Surprise me"
  • Ingredient labels Title Case for consistency
  • Settings menu reorganized: 4 primary sections (Image Settings / Storage / Account / About) + collapsible Developer drawer
  • Force Update renamed "Refresh from Server" — no warning icon, less scary
  • Gear emoji as Settings header anchor
  • Character state badges expanded: ⚠️ untested · 🔁 only generic works · ❌ both failed (each clickable for explanation)
  • New `image_gen_failed_both` flag tracked when fallback retry also fails
  • Tightened ⚠️ keyword list further (only specific names, not categories)
  • Manage Characters: count moved from title to hint line (no overflow); X close button bigger (40x40)
  • Fallback prompt revised: keeps character visually recognizable (hair color, outfit, powers) but rephrases franchise-specific terminology — fixes the over-generic Elsa→Frosthild problem
  • Added "These are ORIGINAL CREATIONS, not references to copyrighted characters" instruction in image prompt when fallback is active
  • Dropped "Children's storybook illustration" from image prompts (was conflicting with selected styles like pixel art, anime, claymation)
  • Style anchor reinforced at END of image prompt too
  • Dropped "do not modify like RedKai" instruction from prompt (was outdated)
  • Age intensity guidance: 1-5 no weapons; 6-7 swords/spells OK no violence; 8-10 mild violence allowed, no gore
  • iPhone landscape safe-area-inset padding (notch / rounded corners no longer cut off buttons)
  • Cover title position fix in portrait (tight grouping with image)
  • Loading image placeholder simplified (centered text + spinner, no dashed box)
  • Auto-commit and auto-push workflow at end of each rev (no longer manual via GitHub Desktop)

--------------------------
v0.6.3 — 2026-05-29 — Form Refinement, Better Reading View, Copyright Fix
  • Artwork Style dropdown added — 9 options (Surprise me / Watercolor / Pencil / Crayon / Comic Book / Anime / Pixel Art / 3D Animation / Claymation). "Surprise me" lets AI pick based on story.
  • Theme: single text input (pills removed)
  • Story Ingredients reduced to 8: Funny, Surprise twist, Heartfelt (combined Heartwarming + Love story), Action-packed, Bedtime, Puzzle, Magical object, Wonder
  • Removed Extra-Long story length (3 options now: Short ~2min / Regular ~4min / Long ~7min)
  • Tightened per-page word counts (Short 65 / Regular 75 / Long 95 words) — prompt now HARD CAPS to ensure no scroll
  • Age picker: 4 stages with emoji (Toddler/Preschool/Early Reader/Older Kid), bigger buttons
  • Capitalized form labels (Title Case throughout)
  • Settings button (⚙️) now lives in the top header bar of BOTH form view and reading view — consistent UI; floating icon removed
  • Removed "New Story" button from reading view (access via Back → form)
  • Reading view top bar simplified: [← Back] [Page X of N] [⚙️]
  • Side arrows for navigation (subtle, on the book pages — not covering text)
  • SWIPE gestures for page navigation (touch horizontal)
  • ⓘ icon now positioned correctly on the IMAGE itself (using aspect-ratio frame), not its wrapper
  • Cover title position fix in portrait — tight grouping with image
  • Story Toolbox: date centered below header, no circles around stars (plain amber), coin sizes less exaggerated (46/48/50/54px vs 38-56)
  • Coin bottom-edge alignment verified
  • Character form: Generic backup name + description fields hidden by default, expandable via "Generic Backup" toggle (cleaner UX)
  • COPYRIGHT FALLBACK NOW HANDLES NAMES TOO: GPT-4o outputs safe_fallback_name; when fallback active, image prompts use both fake name AND fake description. Story text always uses real name.
  • ⚠️ keyword list tightened (specific character names only, no broad terms like "disney" or "princess")
  • Per-character "confirmed_safe" flag set automatically after successful image gen — ⚠️ disappears for proven-safe characters
  • Tap ⚠️ icon: opens warning modal showing what triggered the flag + option to mark safe manually
  • Manage Characters header: "X/5 selected" inline (single line, no overflow)
  • Image inspection still works via ⓘ when Settings toggle is on

--------------------------
v0.6.2 — 2026-05-28 — Settings Menu, Copyright Fallback, Image Quality Upgrades
  • New Settings menu (⚙️ icon top-right) replaces debug bug — organized sections from common to advanced
  • Picture Quality override (Low/Medium/High) for next story; auto-resets to Medium
  • "Show ⓘ button on images" toggle for parents — double-tap removed (was conflicting with iOS zoom)
  • Inspection ⓘ button only shown when toggle is on, lives in corner of image
  • Last Generation costs displayed in kid-friendly format (¢/$)
  • Age picker redesigned: 5 stages with labels + ranges (Toddler 1-2, Preschool 3-4, Early Reader 5-6, Mid Reader 7-8, Older Kid 9-10)
  • Story prompt: age-range guidance + stronger character-name discipline + scene alignment improvements (A: action verbs, B: page text matched exactly, C: composition variety, E: style anchor)
  • TWO-STAGE image prompt enrichment via gpt-4o-mini (closes the "ChatGPT vs API" quality gap — adds composition, lighting, mood)
  • Copyright character fallback flow (FULL):
      - GPT-4o now outputs safe_fallback_visual_description for every character
      - Heuristic detection of possibly-problematic names at creation (⚠️ badge in list)
      - On content_policy error during image gen: modal asks "Try with Generic?" — user confirms, image regenerates with fallback descriptions
      - User confirmation persists per-story for remaining images
      - Per-character "Always use generic" toggle in character edit screen — set automatically after first fallback acceptance ("G" badge in list)
  • Cover prompt fix: no more "Book cover for X" prefix; explicit no-title-text-in-image rule; allow incidental text on clothing/signs
  • Single header bar in reading view (removed bottom bar — more space for the book)
  • Page counter: "Cover" / "Page X of N" / "End" — only story pages numbered
  • Arrow buttons gray out at start/end of book
  • Story Toolbox page (renamed from Story Complete): cover thumbnail + title + creation date in header, rating, less-redundant cost display, action buttons
  • Cost display less redundant: coins + single breakdown line, no duplicate big number
  • Rating stars: all consistent amber, outline when empty, filled when active
  • Coin alignment fixed — all bottom-aligned regardless of size
  • Images take more page area (less padding)
  • Manage Characters: header shows "X/5 selected", "Done" button at bottom, calendar-day date logic with time of day for today/yesterday
  • Character form: shows safe_fallback_visual_description + always-use-fallback toggle
  • Rotate banner removed entirely (portrait mode works fine)
  • Cover title positioning: tighter, grouped with image
  • Landscape scroll alignment: body fixed in reading view to prevent viewport jumping
  • Touch-action: manipulation on images to suppress zoom while allowing taps

--------------------------
v0.6.1 — 2026-05-27 — Reading View Polish
  • Portrait reading mode (image top, text bottom) — replaces the "rotate device" overlay entirely
  • Small dismissible "Best in landscape" hint shows once in portrait mode
  • One unique image per page (no more shared images across pages)
  • Image quality mixed: GPT-4o decides medium (key scenes) or low (simple/transition) per page to keep cost in check
  • Story prompt rewritten: age-scaled intensity — gentler for under 6, real stakes allowed for 7+
  • "Bedtime" ingredient is now the trigger for calming-pacing endings (not the default)
  • Per-page word count target sent to GPT-4o so text fits one screen
  • Strong character-name discipline in prompt (no more "RedKai")
  • Variety in image prompts: not every image needs all characters, focus on key visuals
  • Almost full-bleed images on story spreads (no extra padding around image)
  • Title moved to TOP of cover page
  • Removed "PAGE 4" labels (page counter at bottom is sufficient)
  • "✨ The End ✨" now appears at the bottom of the last text page
  • New "Story Complete" page (replaces "End Credits"): "This book cost X", coin breakdown, text/illustrations split, creation date, 5-star rating, Read Again + Regenerate + Continue Story (disabled) + New Story
  • Kid-friendly cost format: ¢ for under $1, $X.XX for over, <1¢ for sub-cent
  • Partial penny only shown when total cost is under 1¢; otherwise rounds to nearest cent
  • Bottom nav bar with arrows + page counter (replaces visible edge arrows that overlapped content)
  • Invisible edge tap zones still work for navigation
  • Long-press replaced with double-tap (double-click) on image to open inspection — no more conflict with iOS save-image gesture
  • Image inspection modal redesigned: thumbnail at top, compact mobile layout, collapsible sections
  • "+ Add" button on character rows is now "+ Add to Story" (clearer label)
  • Character tagline field — short 3-6 word identifier shown under name in list (e.g. "8-year-old curious boy")
  • Full description renamed to "Expanded Character Profile"
  • Newly created characters sort to top of list with "NEW" badge until first use
  • Bug icon moved to top-right, smaller, more transparent (less in the way)
  • Image draggable attribute disabled to prevent accidental drag/save
  • Scroll-to-top on page navigation and orientation changes

--------------------------
v0.6 — 2026-05-26 — Image Generation + Landscape Reading View
  • Image generation via gpt-image-1 (medium quality, 1024x1024) for cover + each unique story page
  • Smart image distribution: short=3, regular=5, long=6, x-long=7 unique images (+1 cover) — pages can share illustrations
  • GPT-4o now outputs a "style_anchor" per story to keep all images visually consistent
  • Character visual_descriptions injected into every image prompt featuring that character
  • Landscape 2-page spread reading view: text on left, image on right
  • Cover page with title overlay; end credits page with cost breakdown + coins
  • Tap-zone navigation (left/right edges) + visible arrow buttons + page counter
  • Portrait mode shows rotate-device overlay
  • Progressive loading: text generates first, switch to reading view immediately with placeholders, images fill in as they complete
  • Long-press (or right-click) any image: opens inspection modal showing full prompt + cost + per-image regenerate
  • Per-image regenerate from inspection modal
  • Graceful failure: failed images show error placeholder with retry button
  • Auto-retry once on transient (5xx) image API errors
  • "Skip Images" debug toggle: generates text + image prompts only, placeholders shown with the prompt visible (saves $$ during testing)
  • IndexedDB storage for image blobs (much larger capacity than localStorage)
  • Debug panel: image count + total image storage size, clear-all-images button
  • Image alt-text set to image_prompt for accessibility + easy inspection
  • End credits page shows breakdown: text cost + image cost separately
  • Loading hint duration scales by story length (30s for short, 120s for extra-long)

--------------------------
v0.5.2 — 2026-05-26 — Service Worker + Cache Reliability
  • Added Service Worker (sw.js) — network-first for HTML, cache-first for assets, version-tagged caches with auto-cleanup
  • Auto-reload page when new SW takes over (so iPhone webapp updates without manual icon remove/re-add)
  • Cache-control meta tags + cache-busted ?v= query strings on CSS/JS for belt-and-suspenders update reliability
  • Apple touch icon set to appicon-preMVP.png (placeholder until MVP branding pass)
  • Version number now displayed under splash title (live, updates with every rev)
  • "Force Update" button in debug panel — nukes SW + all caches + hard reload for stuck states

--------------------------
v0.5.1 — 2026-05-25 — Character UX Polish
  • Renamed "Enhance Description" → "✨ Bring to Life"; result section now labeled "Character Profile"
  • Character rows redesigned: clear "+ Add" / "✓ Added" button instead of subtle ○/✓ circle
  • Selected rows have stronger visual state (background, border, glow)
  • Max 5 characters selectable per story; cap shown in label and "+ Add" disables to "Max" when reached
  • Better empty state in character list (icon + friendly text)
  • Onboarding hint at top of modal: "Tap a character to add them to your story (up to 5)"
  • Random-character notice clearly states "newly invented but not saved yet"
  • Empty-state hint on main form when no characters selected
  • Character count in modal header: "Manage Characters (3)"
  • Expandable "Character Profile" preview on each row — tap chevron to peek at the full visual description
  • Removed Cancel button from create/edit form (X in header handles same action — consistent UI)
  • "Try Again" replaces "Regenerate" wording for clarity
  • Compact button sizing optimized for iPhone widths

--------------------------
v0.5 — 2026-05-25 — Saved Characters
  • New Manage Characters modal — create, edit, delete, list with most-recent-first sorting
  • Characters stored in localStorage; visual_description used in prompts for consistency
  • "Enhance Description" button polishes a user's short input into a richly detailed AI description
  • Save is gated — must Enhance first before character can be saved
  • "🎲 Generate Random Character" creates a full character profile in one click
  • Multi-select on main form via character chips; per-story role assignment (Good Guy / Bad Guy / None)
  • Relative-time labels ("Used today / 3 days ago / last week / months ago") live-computed
  • Saved characters injected into the story prompt with role + visual_description
  • Story records now store character_ids and character_roles for future library use
  • Removed free-text Characters field from main form (saved characters only)
  • Bug fixes: simpler theme words (Perseverance → Keep trying; Curiosity → Being curious)
  • Bug fix: debug section titles no longer ALL-CAPS (preserves "GPT-4o" casing)
  • Bug fix: Storage display now counts only stories key (reads 0 B after Clear)
  • Debug panel: shows count of saved characters; expanded inspection of selected characters' visual_descriptions

--------------------------
v0.4.2 — 2026-05-23 — Story Ingredients, Coins, Nav, and Prompt Polish
  • Renamed Mood → Story Ingredients (12 options, max 3 selectable with counter)
  • Renamed Anything Else → Story Details with new placeholder
  • Story Details now top-priority in the prompt — overrides other fields when in conflict
  • Added Genre: "Surprise me" (new default) and "Animal Tales" → 10 total
  • Added ingredients: Bedtime, Love Story, Cliffhanger; removed: Dreamy, Hidden Secret
  • Coin cost display in main story view (penny, nickel, dime, quarter; real-world sizes)
  • Partial-penny rendering for under-1¢ cost using CSS clip-path
  • "Back to Story" and "Edit Settings" buttons — navigate between form and story without losing it
  • "Reset Form" button restores defaults
  • "Continue Story" disabled placeholder button (sequels coming)
  • Smart form memory: age + length sticky; genre, ingredients, theme, characters, details all fresh
  • Storage size + "Clear All Stories" button in debug panel
  • Story records now include parent_story_id and story_series_id (future-proofing for sequels)
  • Prompt: cut filler lines; removed bedtime assumption; added priority weighting for Story Details
  • "GPT-4o" capitalization consistent throughout

--------------------------
v0.4.1 — 2026-05-23 — Form Polish
  • Removed Friendship from Genre (kept in Theme — no more cross-category duplicates)
  • Age slider replaced with tappable number pills 1–10
  • Age label changed to "Story for age" (clearer)
  • Added micro-help text under Genre / Mood / Theme labels

--------------------------
v0.4 — 2026-05-23 — Form Simplification + Prompt Quality
  • Form reduced to 7 fields, new order: Characters → Anything Else → Age → Length → Genre → Mood → Theme
  • Characters now free-text (any description or named characters)
  • Anything Else merges old Setting + Special Touches into one box
  • New Mood multi-select (Funny, Surprise twist, Heartwarming, Action-packed, Dreamy)
  • New Theme field: 10 preset pills + custom text input ("the moral of the story is...")
  • Genres rebuilt: Adventure, Fairy Tale, Fantasy, Sci-Fi, Pirates, Superhero, Mystery, Friendship, Spooky
  • Removed: Bedtime/Calming genre, character-type dropdown, separate Setting/Lesson fields, Artwork Style selector (returns with image gen)
  • Length minutes updated: 2 / 5 / 8 / 12
  • Prompt rewritten: quality directives first (cohesion, arc, consistent characters), structure second
  • Auto-retry once silently on malformed JSON
  • All selectors now pill-style (no native HTML dropdowns)
  • Removed emoji from action buttons (kept on content tags)
  • Debug panel: app version + build date displayed
  • Loading hint scales by length

--------------------------
v0.3 — 2026-05-23 — First Working Story Generation
  • Built splash screen with auto-dismiss + tap-to-skip
  • First-launch password prompt; password saved to localStorage per device
  • Full story creation form with inputs:
      age (1–10 slider), length (Short/Regular/Long/Extra-Long with read-time),
      genre, character type, setting, special touches, lesson, artwork style
  • Artwork style selector wired into UI but not yet connected to image gen
  • "Generate Story" → calls Cloudflare Worker → GPT-4o with structured JSON output
  • Loading screen with rotating friendly messages while waiting
  • Story display: long-scroll layout, title + paginated text, per-story cost
  • Regenerate and New Story buttons
  • All generated stories silently saved to localStorage (ready for future library)
  • Debug panel (🐛 icon) showing prompt sent, raw response, tokens, cost
  • "Generate Fake Story" button in debug panel (test UI without API cost)
  • Reset Password button in debug panel
  • Split JS into modules: storage.js, api.js, app.js for clarity

--------------------------
v0.2 — 2026-05-23 — Cloudflare Worker API Proxy
  • Added Cloudflare Worker as secure middleman between app and OpenAI
  • OpenAI API key now lives only in Cloudflare environment, never in code
  • Password-protected access (X-App-Password header required)
  • Worker forwards any /v1/* OpenAI endpoint (text, images, TTS)
  • CORS configured so browser requests are accepted
  • Tested end-to-end: bad password rejected (401), good password forwards to OpenAI ✓
  • Worker source kept in /cloudflare-worker for reference (actual code deployed via Cloudflare dashboard)

v0.1 — 2026-05-23 — Project Scaffold
  • Created initial folder and file structure
  • Set up .gitignore to protect secrets and system files
  • Added placeholder HTML shell (app loads in browser)
  • Established CHANGELOG, README, and project conventions
--------------------------
